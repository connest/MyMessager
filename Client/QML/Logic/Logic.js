function setConnectionParameters(hostname,
                                 name,
                                 password,
                                 clientName,
                                 clientPassword)
{
    qmlconnector.setConnectionData(hostname,
                                   name,
                                   password,
                                   clientName,
                                   clientPassword);

}
